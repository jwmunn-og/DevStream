<div class="row">
  <div class="card small-12 large-10 large-offset-1 columns">
    <div class="row">
      <h2 class="small-12 columns"><%= @course.title %></h2>
      <%= image_tag @course.image %>
      <div class="small-12 large-4 columns text-center">
        <br />
        <span class="callout success">
          <%= number_to_currency @course.cost %>
        </span>
        <br class="clear" />
        <br />
        <% if current_user && current_user == @course.user %>
          <%= link_to 'Administer', instructor_course_path(@course), class: 'button alert' %>
        <% end %>
      </div>
      <div class="small-12 large-8 columns">
        <br />
        <%= @course.description %>
      </div>
      <div class="small-12 large-8 columns">
        <hr />
        <% @course.sections.each do |section| %>
          <div class="section">
              <h3><%= section.title %></h3>
              <% section.lessons.each do |lesson| %>
                <div class="lesson">
                  <%= link_to lesson_path(lesson) do %>
                    <strong><%= lesson.title %></strong> - <%= lesson.subtitle %>
                  <% end %>
                </div>
              <% end %>
              <hr />
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
